# AgriShield X - Environment Variables Configuration

# Google Gemini API Key (Optional - Fallback to deterministic AI demo mode if omitted)
GEMINI_API_KEY=

# Server Configuration
PORT=5000
NODE_ENV=development

# Weather API Key (Optional - Fallback to Open-Meteo free API / demo data if omitted)
WEATHER_API_KEY=
